# paths
results_dir: ./results

phase: test

shuffle_identities: True
shuffle: False 
no_flip: True
display_id: -1
print_freq: 5

eval: True

num_threads: 0
batch_size: 64
sample_window: [500, 100] # the second parameter is sampling frequency, first is offset for cond_A

used_points: all
error_form: image_size

num_test_save: 6000
skeleton_subset_size: 0

offline_regressor: True
